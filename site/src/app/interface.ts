
export interface IKeyPress {
    UP: boolean,
    DOWN: boolean,
    LEFT: boolean,
    RIGHT: boolean
}

export interface ISkyBoxMapping {
    posx: string
    posy: string
    posz: string
    negx: string
    negy: string
    negz: string
}